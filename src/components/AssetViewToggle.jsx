// @ts-ignore;
import React from 'react';
// @ts-ignore;
import { Button, ToggleGroup, ToggleGroupItem } from '@/components/ui';

// 这个组件现在被整合到PortfolioChart中，可以删除或保留为空
export function AssetViewToggle({
  timeRange,
  setTimeRange,
  displayMode,
  setDisplayMode
}) {
  return null;
}